void Init(Int x, Int y, Int w, Int h) {
	Atom S = new(Empty);

	if (random_oneIn(100)) {
		S = new(Animal);
	} else if (random_oneIn(50)) {
		S = new(Plant);
	} else if (random_oneIn(10)) {
		S = new(Res);
	}
	
	ew(0, S); 
}