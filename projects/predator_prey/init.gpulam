void Init(C2D c, S2D s) {
	Atom S = new(Empty);

	if (random_oneIn(100)) {
		S = new(Animal);
	} else if (random_oneIn(10)) {
		S = new(Plant);
	}// else if (random_oneIn(10)) {
	//	S = new(Res);
	//}
	
	ew(0, S); 
}